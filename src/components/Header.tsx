import Logo from "@/assets/Logo";

const Header = () => {
	return (
		<header className="bg-black flex justify-center p-10">
			<Logo className="animate-show-element" />
		</header>
	);
};

export default Header;
